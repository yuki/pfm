<%= form_for(@mtype, :html=>{:class=>"form-horizontal"}) do |f|%>
  <% if @mtype.errors.any? %>
    <div id="error_explanation">
      <div class="alert alert-danger"><%= pluralize(@mtype.errors.count, "error") %> prohibited this mtype from being saved:</div>

      <ul>
      <% @mtype.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name, :class=>"col-sm-2 control-label" %>
    <div class="col-sm-2">
      <%= f.text_field :name, :class=>"form-control" %>
    </div>
  </div>
  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
    <%= f.submit :class=>"btn btn-info" %>
    </div>
  </div>

<% end %>
